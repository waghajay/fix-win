/*
Statement: Find which is bigger.
Expected Output: 20 is big
*/

#include <stdio.h>
int main() {
    int x = 10, y = 20;
    if (x > y)
        printf("%d is big", x);
    else (y > x)
        printf("%d is big", y);
    return 0;
}
